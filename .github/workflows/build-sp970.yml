name: Build_sp970
on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compile
        run: |
          echo "Starting full build..."
          # 这里会自动调用 scripts/ 里的编译脚本
          # 包含你要求的 daed, passwall, istore 等
